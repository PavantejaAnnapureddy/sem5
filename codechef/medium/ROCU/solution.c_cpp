#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;

int main() {
    int N;
    cin >> N;
    
    vector<int> price(N + 1);
    for (int i = 1; i <= N; i++) {
        cin >> price[i];
    }
    
    vector<long long> dp(N + 1, 0);
    
    for (int i = 1; i <= N; i++) {
        for (int j = 1; j <= i; j++) {
            dp[i] = max(dp[i], price[j] + dp[i - j]);
        }
    }
    
    cout << dp[N] << endl;
    return 0;
}